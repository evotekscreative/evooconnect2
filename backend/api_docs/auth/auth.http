###############################################################
# Authentication API HTTP Request Examples
# This file provides examples of requests to the authentication API
# It can be used with the REST Client extension in VS Code
###############################################################

### 1. Registration and Account Verification
###############################################################

### Register new user
# Creates a new user account and sends a verification email
# Response: 201 Created with JWT token and user data
POST http://localhost:3000/api/auth/register
Accept: application/json
Content-Type: application/json

{
  "name": "Test User",
  "email": "test.user@example.com",
  "password": "password123"
}

### Send verification email
# Sends a 6-digit code to the user's email for verification
# Limited to 3 requests per 15 minutes per IP address
# Response: 200 OK with confirmation message
POST http://localhost:3000/api/auth/verify/send
Accept: application/json
Content-Type: application/json

{
  "email": "test.user@example.com"
}

### Verify email with token
# Verifies email address using the 6-digit token
# Token is valid for 24 hours
# Limited to 10 attempts per 5 minutes per IP address
# Response: 200 OK when verified
POST http://localhost:3000/api/auth/verify
Accept: application/json
Content-Type: application/json

{
  "token": "123456"
}

### 2. Authentication and Session Management
###############################################################

### Login
# Authenticates user and returns JWT token
# Email must be verified before login is permitted
# Response: 200 OK with JWT token and user data
POST http://localhost:3000/api/auth/login
Accept: application/json
Content-Type: application/json

{
  "email": "test.user@example.com",
  "password": "password123"
}

### 3. Password Recovery
###############################################################

### Request password reset
# Sends a 6-digit reset code to user's email
# Limited to 3 requests per 15 minutes per IP address
# Response: 200 OK with confirmation message
POST http://localhost:3000/api/auth/forgot-password
Accept: application/json
Content-Type: application/json

{
  "email": "test.user@example.com"
}

### Reset password with token
# Resets password using the 6-digit token
# Token is valid for 1 hour
# Response: 200 OK when password is successfully reset
POST http://localhost:3000/api/auth/reset-password
Accept: application/json
Content-Type: application/json

{
  "token": "123456",
  "password": "new-password123"
}

###############################################################
# Response Examples:
#
# Success Response:
# {
#   "code": 200,
#   "status": "OK",
#   "data": {
#     "message": "Operation successful"
#     // or other data specific to the endpoint
#   }
# }
#
# Error Response:
# {
#   "code": 400, // or 401, 404, 429 etc.
#   "status": "BAD REQUEST", // or "UNAUTHORIZED", "NOT FOUND", etc.
#   "data": "Error message" // or validation error object
# }
###############################################################